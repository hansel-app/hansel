syntax = "proto3";
package usersapi;
option go_package = "/usersapi";

service UsersService {
	rpc getProfile (ProfileRequest) returns (ProfileResponse);
}

message ProfileRequest {
	int64 userId = 1;
}

message ProfileResponse {
	string name = 1;
	string username = 2;
	repeated Profile friends = 3;
}
